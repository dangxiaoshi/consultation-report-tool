# 开发日志

## 2026-04-08

### 本次变更
- 将 GitHub 仓库 `dangxiaoshi/consultation-report-tool` 从私有改为公开。
- 新增首页文件 `index.html`，用于 GitHub Pages 直接发布访问。
- 开启 GitHub Pages，发布源为 `main` 分支根目录。

### 发布结果
- 仓库地址：<https://github.com/dangxiaoshi/consultation-report-tool>
- 公开页面：<https://dangxiaoshi.github.io/consultation-report-tool/>
- Pages 构建状态：`built`
- 首页提交：`552b749` `Add index page for GitHub Pages`

### 说明
- 当前页面是单文件静态 HTML，可直接托管，无需额外构建流程。
- 页面内包含前端明文 `DeepSeek` API Key，仓库公开后任何人都可查看和调用。
- 由于本次目标是“直接公开并可被其他人访问”，因此保留当前实现，不做密钥隔离。

### 后续可选优化
- 将 API Key 挪到后端或 Worker，避免公开滥用。
- 补充 `README.md`，说明使用方式、风险和部署方法。
- 绑定自定义域名，替换 GitHub Pages 默认地址。
